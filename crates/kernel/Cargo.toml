[package]
name = "kandelo"
version.workspace = true
edition.workspace = true

[lib]
name = "kandelo_kernel"
crate-type = ["cdylib", "rlib"]

[dependencies]
dlmalloc = "0.2.14"
wasm-posix-shared = { path = "../shared" }
